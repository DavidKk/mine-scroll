/** @deprecated 使用 `core/modes/engine.ts` */
export {
  chordAt as chordReveal,
  createSession as createGame,
  getFlagCount,
  revealAt as reveal,
  toCellViews,
  toggleMarkAt as toggleFlag,
} from './modes/engine.ts';
